import { createStore } from 'vuex'

export default createStore({
    state: {
        isAuthenticated: true,
        aif_loginToken: '',
    },
    mutations: {
    },
    actions: {
    },
    modules: {
    }
})
